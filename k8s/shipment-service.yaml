apiVersion: v1
kind: Service

metadata:
  name: shipment-service
  namespace: smart-logistics

spec:
  selector:
    app: shipment-service

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081

  type: NodePort